//crear un formulario que permita validar el correo electronico si el email no es valido, 
//mostrara un mensaje de error dinamico bajo el campo de texto

import divForm from './components/divForm.js';
import formComponent from './components/formComponent.js';
document.addEventListener('DOMContentLoaded', function() {
    //Crear un contenedor para mi formulario
    const app = document.getElementById('app');
    // const divform = divForm();
    // app.appendChild(divform);


    
})